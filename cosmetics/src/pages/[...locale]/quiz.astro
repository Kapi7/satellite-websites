---
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import BackToTop from '../../components/BackToTop.astro';
import { locales, defaultLocale, type Locale } from '../../i18n/locales';
import { t } from '../../i18n/utils';

export function getStaticPaths() {
  return locales.map(l => ({
    params: { locale: l === defaultLocale ? undefined : l },
    props: { locale: l },
  }));
}

const { locale } = Astro.props as { locale: Locale };
---

<Base title={`${t(locale, 'quiz.title')} | Glow Coded`} description={t(locale, 'quiz.description')} locale={locale}>
  <Header locale={locale} />
  <main class="flex-1">
    <section class="gcd-tool-wrap">
      <div class="gcd-tool-head animate-fade-in">
        <div class="gcd-kicker">
          <span class="pill">&#10022; TOOL 01</span>
          <span>Skin Quiz &middot; 2 minutes</span>
        </div>
        <h1 class="gcd-tool-title">
          {t(locale, 'quiz.title').split(' ').slice(0, -1).join(' ')} <em>{t(locale, 'quiz.title').split(' ').slice(-1)[0]}</em>
        </h1>
        <p class="gcd-tool-lede">{t(locale, 'quiz.description')}</p>
      </div>

      <div id="quiz-container" class="gcd-tool-card animate-fade-in-up stagger-1">
        <iframe
          id="quiz-iframe"
          src="https://mirai-skin-ai.onrender.com/?shop=9dkd2w-g3.myshopify.com&currency=USD&embedded=true&inline=true"
          class="w-full border-0 block"
          style="height: 680px;"
          allow="camera"
          title={t(locale, 'quiz.title')}
        ></iframe>
      </div>

      <p class="text-center text-[11px] text-plum-300 mt-4">
        {t(locale, 'quiz.poweredByAi')}
      </p>
    </section>
  </main>
  <Footer locale={locale} />
  <BackToTop />

  <script is:inline>
    (function() {
      var iframe = document.getElementById('quiz-iframe');
      var container = document.getElementById('quiz-container');

      window.addEventListener('message', function(e) {
        if (!e.data || !e.data.type) return;

        if (e.data.type === 'mirai:resize' && e.data.height) {
          var h = Math.min(Math.max(500, e.data.height), 800);
          iframe.style.height = h + 'px';
        }

        if (e.data.type === 'mirai:scroll-top') {
          container.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    })();
  </script>
</Base>
