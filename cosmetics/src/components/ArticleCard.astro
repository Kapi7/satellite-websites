---
interface Props {
  title: string;
  description: string;
  date: Date;
  category: string;
  slug: string;
  image?: string;
  type?: string;
  featured?: boolean;
}

const { title, description, date, category, slug, image, type, featured = false } = Astro.props;

const categoryLabels: Record<string, string> = {
  'skincare': 'Skincare',
  'ingredients': 'Ingredients',
  'reviews': 'Reviews',
  'how-tos': 'How-Tos',
};

const typeLabels: Record<string, string> = {
  hub: 'Essential Guide',
  guide: 'Guide',
  listicle: 'List',
  review: 'Review',
  routine: 'Routine',
};

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---

<article class={`group ${featured ? 'md:col-span-2' : ''}`}>
  <a href={`/${slug}`} class="block h-full">
    {image && (
      <div class={`overflow-hidden rounded-2xl mb-6 bg-rose-50 ${featured ? 'aspect-[16/9]' : 'aspect-[3/2]'}`}>
        <img
          src={image}
          alt={title}
          class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
          loading="lazy"
          width="600"
          height="400"
        />
      </div>
    )}
    <div class="flex items-center gap-2.5 mb-3">
      <span class="text-xs tracking-[0.15em] uppercase text-plum-400 mb-3 block">
        {categoryLabels[category] || category}
      </span>
      {type && (
        <>
          <span class="w-1 h-1 rounded-full bg-rose-300"></span>
          <span class="text-[11px] font-medium uppercase tracking-[0.08em] text-rose-500">
            {typeLabels[type] || type}
          </span>
        </>
      )}
    </div>
    <h3 class={`font-heading group-hover:text-rose-700 transition-colors leading-snug text-charcoal tracking-tight ${featured ? 'text-3xl' : 'text-xl'}`}>
      {title}
    </h3>
    <p class="text-charcoal-light/70 text-sm mb-4 line-clamp-2">
      {description}
    </p>
    <time class="text-xs text-plum-300 uppercase tracking-widest" datetime={date.toISOString()}>
      {formattedDate}
    </time>
  </a>
</article>
